---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
---

<Layout title="NAL | Estudio Jurídico Especializado">
  <Navbar />
  
  <main>
    <section id="inicio" class="hero">
      <div class="hero-watermark">Nadia</div>
      
      <div class="hero-bg-accent"></div>
      
      <div class="container hero-content">
        <div class="hero-label fade-in">
          <span class="label-line"></span>
          <span>Abogada UBA · Matrícula Federal</span>
        </div>

        <h1 class="fade-in delay-1">
          Tu defensa es algo <br/>
          <span class="text-script">Personal</span>
        </h1>
        
        <p class="lead fade-in delay-2">
          Estudio jurídico boutique especializado en <strong>Accidentes de Trabajo (ART)</strong>. 
          No sos un número de expediente, sos una prioridad.
        </p>
        
        <div class="hero-actions fade-in delay-3">
          <a href="#contacto" class="btn-primary">Consulta Gratuita</a>
          <a href="#especialidad" class="btn-text">
            <span>Nuestras Áreas</span>
            <span class="arrow-down">↓</span>
          </a>
        </div>
      </div>
    </section>

    <section class="statement-section">
      <div class="container text-center">
        <h2>No somos una fábrica de <span class="text-script" style="font-size: 1.3em;">Expedientes.</span></h2>
        <p class="statement-lead">
          En un sistema legal masificado, elegimos limitar nuestra cantidad de casos para garantizar una defensa artesanal y personalizada a cada trabajador.
        </p>
      </div>
    </section>

    <section id="especialidad" class="section-padding">
      <div class="container">
        <div class="section-header">
          <p class="overline">Enfoque Exclusivo</p>
          <h2>Expertos en <span class="text-accent">A.R.T.</span></h2>
          <p class="section-desc">Nos enfocamos en una sola rama para garantizar la máxima indemnización.</p>
        </div>

        <div class="bento-grid">
          <article class="card large">
            <div class="card-bg-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="0.5"><path d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
            </div>
            <div class="card-content">
              <div class="icon-box">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" /></svg>
              </div>
              <h3>Accidentes Laborales</h3>
              <p>Caídas, golpes o lesiones dentro del establecimiento. Reclamamos cobertura médica inmediata, salarios caídos e indemnización integral por incapacidad.</p>
            </div>
          </article>

          <article class="card">
             <div class="card-content">
              <div class="icon-box">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M9 6.75V15m6-6v8.25m.503 3.498 4.875-2.437c.381-.19.622-.58.622-1.006V4.82c0-.836-.88-1.38-1.628-1.006l-3.869 1.934c-.317.159-.69.159-1.006 0L9.503 3.252a1.125 1.125 0 0 0-1.006 0L3.622 5.689C3.24 5.88 3 6.27 3 6.695V19.18c0 .836.88 1.38 1.628 1.006l3.869-1.934c.317-.159.69-.159 1.006 0l4.994 2.497c.317.159.69.159 1.006 0z" /></svg>
              </div>
              <h3>In Itinere</h3>
              <p>Accidentes ocurridos en el trayecto directo de ida o vuelta al trabajo.</p>
            </div>
          </article>

          <article class="card">
             <div class="card-content">
              <div class="icon-box">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" /></svg>
              </div>
              <h3>Enfermedades</h3>
              <p>Hernias, túnel carpiano y patologías por tareas repetitivas.</p>
            </div>
          </article>

          <article class="card tall featured-card">
            <div class="card-content">
              <div class="icon-box">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M12 3v17.25m0 0c-1.472 0-2.882.265-4.185.75M12 20.25c1.472 0 2.882.265 4.185.75M18.75 4.97A47.654 47.654 0 0 0 12 4.5c-2.291 0-4.545.16-6.75.47m13.5 0c1.01.143 2.01.317 3 .52m-3-.52 2.62 10.726c.122.499-.106 1.028-.589 1.202a5.988 5.988 0 0 1-2.031.352 5.988 5.988 0 0 1-2.031-.352c-.483-.174-.711-.703-.59-1.202L18.75 4.971zm-16.5.52c.99-.203 1.99-.377 3-.52m0 0 2.62 10.726c.122.499-.106 1.028-.589 1.202a5.988 5.988 0 0 1-2.031.352 5.988 5.988 0 0 1-2.031-.352c-.483-.174-.711-.703-.59-1.202L5.25 4.971z" /></svg>
              </div>
              <h3>Despidos &<br>Rechazos</h3>
              <p>¿La ART rechazó tu accidente? ¿Te dieron el alta médica antes de tiempo? Iniciamos las acciones legales para revertirlo.</p>
              <a href="#contacto" class="btn-primary full-width" style="margin-top: auto;">Iniciar Reclamo</a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="stats-section">
      <div class="container stats-grid">
        <div class="stat-item">
          <span class="stat-number">+8</span>
          <span class="stat-label">Años de Experiencia</span>
        </div>
        <div class="stat-item">
          <span class="stat-number">UBA</span>
          <span class="stat-label">Título de Grado</span>
        </div>
        <div class="stat-item">
          <span class="stat-number">100%</span>
          <span class="stat-label">Dedicación ART</span>
        </div>
      </div>
    </section>
    
    <section class="bio-section section-padding">
      <div class="container bio-layout">
        <div class="bio-text">
          <h2>Dra. Nadia A. López</h2>
          <p class="bio-subtitle">Fundadora & Directora</p>
          <div class="divider-accent"></div>
          <p>
            Como egresada de la <strong>Universidad de Buenos Aires</strong>, fundé este estudio con una misión clara: equilibrar la balanza entre el trabajador y las aseguradoras.
          </p>
          <p>
            Entiendo que detrás de un accidente no hay solo un expediente, sino una persona cuya salud y sustento están en riesgo. Por eso mi lema es <em>"Tu caso, nuestro compromiso"</em>. No delegamos a juniors; yo superviso personalmente tu reclamo.
          </p>
        </div>
        <div class="bio-visual">
            <div class="frame-image">
                <div class="img-placeholder">NAL</div>
            </div>
        </div>
      </div>
    </section>

    <section class="faq-section section-padding">
      <div class="container">
        <div class="faq-header text-center">
          <p class="overline">Tus Derechos</p>
          <h2>Respuestas <span class="text-script">Claras</span></h2>
        </div>

        <div class="accordion">
          <div class="accordion-item">
            <button class="accordion-header" aria-expanded="false">
              <span>¿Tengo que pagar para iniciar el reclamo?</span>
              <span class="icon-plus">+</span>
            </button>
            <div class="accordion-content">
              <p>
                <strong>Absolutamente nada.</strong> En el fuero laboral y ART rige el beneficio de gratuidad. 
                Mis honorarios los paga la aseguradora al finalizar el juicio o acuerdo. Vos no ponés un peso de tu bolsillo.
              </p>
            </div>
          </div>

          <div class="accordion-item">
            <button class="accordion-header" aria-expanded="false">
              <span>¿Pueden despedirme por reclamar a la ART?</span>
              <span class="icon-plus">+</span>
            </button>
            <div class="accordion-content">
              <p>
                <strong>No deberían.</strong> El reclamo es contra la Aseguradora (ART), no contra tu empleador. 
                Tu empleador paga un seguro justamente para cubrir estos eventos. Si te despidieran por esto, sería un despido discriminatorio y la indemnización se duplica.
              </p>
            </div>
          </div>

          <div class="accordion-item">
            <button class="accordion-header" aria-expanded="false">
              <span>¿Qué pasa si estoy trabajando "en negro"?</span>
              <span class="icon-plus">+</span>
            </button>
            <div class="accordion-content">
              <p>
                Tenés los mismos derechos. Si sufriste un accidente y no estás registrado, tu empleador es responsable directo. 
                Podemos reclamar tanto la regularización laboral como la indemnización por el accidente.
              </p>
            </div>
          </div>

          <div class="accordion-item">
            <button class="accordion-header" aria-expanded="false">
              <span>La ART me dio el alta pero sigo con dolores</span>
              <span class="icon-plus">+</span>
            </button>
            <div class="accordion-content">
              <p>
                Es la situación más común. Las ART suelen dar altas prematuras para ahorrar costos. 
                Tenés derecho a impugnar esa alta médica y exigir que te sigan tratando o que te paguen la indemnización por la secuela (incapacidad) que te quedó.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="contacto" class="section-padding contact-section">
      <div class="contact-glow"></div>

      <div class="container">
        <div class="contact-layout">
          
          <div class="contact-info">
            <p class="overline">Consulta Gratuita</p>
            <h2>No enfrentes a la ART <span class="text-script">en soledad.</span></h2>
            <p class="contact-lead">
              Sabemos cómo operan las aseguradoras. Contame tu caso hoy y recibí una respuesta directa de una abogada especialista, sin intermediarios.
            </p>
            
            <div class="contact-methods">
              <div class="method-item">
                <div class="method-icon">
                  <svg fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" /><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" /></svg>
                </div>
                <div>
                  <h4>Oficina Central</h4>
                  <p>Av. Corrientes y Florida, CABA</p>
                </div>
              </div>
              
              <div class="method-item">
                <div class="method-icon">
                  <svg fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 0 0 2.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 0 1-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 0 0-1.091-.852H4.5A2.25 2.25 0 0 0 2.25 4.5v2.25Z" /></svg>
                </div>
                <div>
                  <h4>Atención Inmediata</h4>
                  <p>11 1234 5678 (WhatsApp)</p>
                </div>
              </div>

              <div class="method-item">
                <div class="method-icon">
                  <svg fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 0 1-2.25 2.25h-15a2.25 2.25 0 0 1-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25m19.5 0v.243a2.25 2.25 0 0 1-1.07 1.916l-7.5 4.615a2.25 2.25 0 0 1-2.36 0L3.32 8.91a2.25 2.25 0 0 1-1.07-1.916V6.75" /></svg>
                </div>
                <div>
                  <h4>Email</h4>
                  <p>consultas@nalestudio.com.ar</p>
                </div>
              </div>
            </div>
          </div>
          
          <div class="form-wrapper">
            <form class="boutique-form">
              <div class="form-header">
                  <h3>Iniciar Consulta</h3>
                  <p>Confidencialidad absoluta.</p>
              </div>

              <div class="input-group">
                <input type="text" required placeholder=" " />
                <label>Nombre Completo</label>
              </div>

              <div class="input-group">
                <input type="tel" required placeholder=" " />
                <label>Teléfono / WhatsApp</label>
              </div>

              <div class="input-group">
                <select required>
                    <option value="" disabled selected>Tipo de Accidente</option>
                    <option value="trabajo">En el trabajo</option>
                    <option value="itinere">Yendo/Volviendo (In Itinere)</option>
                    <option value="enfermedad">Enfermedad Profesional</option>
                    <option value="despido">Despido por Accidente</option>
                </select>
              </div>

              <div class="input-group">
                <textarea rows="3" required placeholder=" "></textarea>
                <label>Breve relato del caso</label>
              </div>

              <button type="submit" class="btn-primary full-width">
                <span>Enviar a Nadia</span>
                <span class="btn-icon">→</span>
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

  </main>
  
  <footer class="site-footer">
    <div class="container">
      <div class="footer-content">
        
        <div class="footer-brand">
          <div class="brand-logo">
            <span class="logo-script">N.A.L.</span>
            <span class="logo-subtitle">Estudio Jurídico</span>
          </div>
          <p class="brand-mission">
            Defensa legal con perspectiva humana. Especialistas en hacer valer los derechos del trabajador frente a las grandes aseguradoras.
          </p>
        </div>

        <div class="footer-links">
          <h4>Navegación</h4>
          <ul>
            <li><a href="#inicio">Inicio</a></li>
            <li><a href="#especialidad">Áreas de Práctica</a></li>
            <li><a href="#contacto">Solicitar Consulta</a></li>
          </ul>
        </div>

        <div class="footer-contact">
          <h4>Oficinas</h4>
          <div class="contact-row">
            <span class="icon">📍</span>
            <p>Av. Corrientes 1234, Piso 8<br>CABA, Buenos Aires</p>
          </div>
          <div class="contact-row">
            <span class="icon">📱</span>
            <p>11 1234-5678 (Urgencias)</p>
          </div>
          <div class="contact-row">
            <span class="icon">✉️</span>
            <p>contacto@nalestudio.com.ar</p>
          </div>
        </div>

      </div>

      <div class="footer-bottom">
        <div class="divider"></div>
        <div class="bottom-flex">
            <p>© {new Date().getFullYear()} Nadia A. López. Todos los derechos reservados.</p>
            <p class="credits">Diseño y Estrategia: <a href="#" target="_blank">VisionPath</a></p>
        </div>
      </div>
    </div>
  </footer>
</Layout>

<style>
  /* =========================================
     0. BLINDAJE ANTI-DESBORDE (CRÍTICO)
     ========================================= */
  
  /* 1. Reset Global: Nada puede ser más ancho que la pantalla */
  html, body {
    max-width: 100%;
    overflow-x: hidden !important; /* Fuerza bruta para cortar el scroll lateral */
    position: relative;
  }

  /* 2. Control de Padding y Box Model */
  *, *::before, *::after {
    box-sizing: border-box;
  }

  /* ======================
     ESTILOS ESPECÍFICOS NAL
     ====================== */
  
  /* HERO */
  .hero { 
    min-height: 95vh; 
    display: flex; 
    align-items: center; 
    position: relative; 
    padding-top: var(--nav-height); 
    /* IMPORTANTE: Esto corta la marca de agua y el blur si se salen */
    overflow: hidden; 
    width: 100%;
  }

  /* Marca de agua (Nadia) */
  .hero-watermark {
    position: absolute;
    top: 15%;
    left: 50%;
    transform: translateX(-50%) rotate(-5deg);
    font-family: var(--font-logo); 
    font-size: 15rem; 
    color: rgba(255, 255, 255, 0.03); 
    pointer-events: none;
    z-index: 0;
    white-space: nowrap;
    /* Aseguramos que no empuje el ancho */
    max-width: 100vw;
    display: flex;
    justify-content: center;
    overflow: hidden; 
  }

  /* Mancha de Luz */
  .hero-bg-accent {
    position: absolute; 
    top: -30%; 
    right: -10%; 
    width: 600px; 
    height: 600px;
    background: radial-gradient(circle, rgba(224, 195, 232, 0.12) 0%, transparent 60%);
    filter: blur(80px); 
    z-index: 0; 
    pointer-events: none;
  }

  .hero-content { position: relative; z-index: 2; width: 100%; max-width: 1000px; }

  /* Texto Script "Personal" */
  .text-script {
    font-family: var(--font-logo);
    color: var(--color-accent);
    font-weight: 400;
    font-size: 1.3em; 
  }

  .hero-label { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.5rem; color: var(--color-accent); font-size: 0.8rem; text-transform: uppercase; letter-spacing: 2px; }
  .label-line { width: 40px; height: 1px; background: var(--color-accent); }

  h1 { font-size: clamp(3rem, 6vw, 5rem); margin-bottom: 1.5rem; }
  .lead { font-size: 1.1rem; color: var(--color-text-muted); max-width: 550px; margin-bottom: 3rem; line-height: 1.6; }
  .hero-actions { display: flex; gap: 2rem; align-items: center; }

  /* ÁREAS (Iconos) */
  .section-padding { padding: 5rem 0; }
  .section-header { margin-bottom: 3rem; }
  .overline { color: var(--color-accent); text-transform: uppercase; letter-spacing: 2px; font-size: 0.8rem; font-weight: 700; }
  .section-desc { color: var(--color-text-muted); margin-top: 1rem; }

  .bento-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  
  .card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(224, 195, 232, 0.1);
    border-radius: 12px; 
    padding: 2rem;
    position: relative;
    overflow: hidden; /* Corta el icono gigante de fondo */
    transition: all 0.3s ease;
  }
  .card:hover { border-color: var(--color-accent); background: rgba(255, 255, 255, 0.05); }
  
  .icon-box {
    width: 50px; height: 50px; background: rgba(224, 195, 232, 0.1); border-radius: 50%;
    display: flex; align-items: center; justify-content: center; margin-bottom: 1.5rem;
    color: var(--color-accent); border: 1px solid rgba(224, 195, 232, 0.2); transition: all 0.3s ease;
  }
  .card:hover .icon-box { background: var(--color-accent); color: var(--color-bg); transform: scale(1.1); }
  
  .card-bg-icon {
    position: absolute; right: -20px; bottom: -20px; width: 150px; height: 150px;
    color: rgba(255,255,255,0.03); transform: rotate(-15deg); pointer-events: none; transition: transform 0.5s ease;
  }
  .card:hover .card-bg-icon { transform: rotate(0deg) scale(1.1); color: rgba(255,255,255,0.05); }

  .featured-card { background: linear-gradient(145deg, rgba(255,255,255,0.05) 0%, rgba(224, 195, 232, 0.05) 100%); border-color: rgba(224, 195, 232, 0.3); }

  .large { grid-column: span 2; }
  .tall { grid-row: span 2; }
  
  .card h3 { font-size: 1.4rem; color: #fff; margin-bottom: 1rem; }
  .card p { color: var(--color-text-muted); font-size: 0.95rem; line-height: 1.5; }

  /* BIO SECTION */
  .bio-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center; }
  .bio-subtitle { color: var(--color-text-muted); text-transform: uppercase; letter-spacing: 2px; font-size: 0.9rem; }
  .divider-accent { width: 60px; height: 3px; background: var(--color-accent); margin: 1.5rem 0; }
  .bio-text p { color: var(--color-text-muted); line-height: 1.8; margin-bottom: 1rem; }
  .frame-image { width: 100%; height: 400px; border: 1px solid var(--color-accent); padding: 1rem; position: relative; border-radius: 200px 200px 0 0; }
  .img-placeholder { width: 100%; height: 100%; background: linear-gradient(to bottom, #3d4666, #2a324b); border-radius: 190px 190px 0 0; display: flex; align-items: center; justify-content: center; font-family: var(--font-logo); font-size: 4rem; color: var(--color-accent); }

  /* ======================
     STATS SECTION (MEJORADA)
     ====================== */
  .stats-section {
    /* Fondo más oscurito como pediste */
    background: rgba(0, 5, 15, 0.3); 
    border-top: 1px solid rgba(224, 195, 232, 0.1); 
    border-bottom: 1px solid rgba(224, 195, 232, 0.1); 
    padding: 4rem 0; 
    position: relative;
    backdrop-filter: blur(10px); /* Efecto vidrio sutil */
  }

  .stats-grid { 
    display: flex; 
    justify-content: space-between; 
    text-align: center;
    max-width: 900px; /* Para que no queden tan separados en pantallas gigantes */
    margin: 0 auto;
  }

  .stat-item {
    flex: 1; /* Todos ocupan el mismo ancho */
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  /* Líneas divisorias verticales (Solo Desktop) */
  @media (min-width: 900px) {
    .stat-item:not(:last-child)::after {
      content: '';
      position: absolute;
      right: 0;
      top: 10%; /* No toca los bordes, queda flotando */
      height: 80%;
      width: 1px;
      background: linear-gradient(to bottom, transparent, rgba(255,255,255,0.1), transparent);
    }
  }

  .stat-number { 
    display: block; 
    font-family: var(--font-heading); /* Playfair */
    font-size: 3.5rem; /* Más grande e imponente */
    color: var(--color-accent); 
    line-height: 1;
    margin-bottom: 0.5rem;
    text-shadow: 0 0 20px rgba(224, 195, 232, 0.3); /* Brillo suave */
  }

  .stat-label { 
    font-size: 0.85rem; 
    color: #fff; /* Blanco en vez de gris para más contraste sobre lo oscuro */
    opacity: 0.8;
    text-transform: uppercase;
    letter-spacing: 2px; 
    font-weight: 400;
  }

  /* Ajuste Mobile */
  @media (max-width: 900px) {
    .stats-grid { 
      flex-direction: column; 
      gap: 3rem; 
    }
    
    /* Líneas divisorias horizontales en mobile */
    .stat-item:not(:last-child)::after {
      width: 50%;
      height: 1px;
      top: auto;
      bottom: -1.5rem;
      right: auto; /* Centrado */
      background: rgba(255,255,255,0.1);
    }
  }

  /* ======================
     CONTACTO BOUTIQUE (FIXED)
     ====================== */
  .contact-section {
    position: relative;
    overflow: hidden; /* Seguridad extra */
  }

  /* Luz de fondo sutil detrás del contacto */
  .contact-glow {
    position: absolute;
    bottom: 0; left: 0;
    width: 100%; height: 100%;
    background: radial-gradient(circle at bottom left, rgba(224, 195, 232, 0.08) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
  }

  .contact-layout {
    display: grid;
    grid-template-columns: 1fr 1fr; /* Mitad y mitad */
    gap: 6rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  /* Columna Izquierda */
  .contact-lead {
    font-size: 1.1rem;
    color: var(--color-text-muted);
    margin-bottom: 3rem;
    max-width: 450px;
    line-height: 1.6;
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .method-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .method-icon {
    width: 45px; height: 45px;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(224, 195, 232, 0.2);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: var(--color-accent);
  }
  .method-icon svg { width: 22px; height: 22px; }

  .method-item h4 { font-size: 0.9rem; color: #fff; margin-bottom: 0.2rem; }
  .method-item p { margin: 0; font-size: 0.95rem; color: var(--color-text-muted); }

  /* Columna Derecha: El Formulario */
  .form-wrapper {
    position: relative;
  }
  
  /* Borde decorativo "Boutique" alrededor del form */
  .form-wrapper::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: 20px;
    padding: 1px;
    background: linear-gradient(to bottom, var(--color-accent), transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0.3;
    pointer-events: none;
  }

  .boutique-form {
    background: rgba(15, 22, 35, 0.6); /* Fondo oscuro semitransparente */
    backdrop-filter: blur(20px); /* Efecto vidrio */
    padding: 3rem;
    border-radius: 20px;
    box-shadow: 0 20px 50px rgba(0,0,0,0.2);
  }

  .form-header { margin-bottom: 2rem; text-align: center; }
  .form-header h3 { color: #fff; font-size: 1.8rem; margin-bottom: 0.5rem; }
  .form-header p { color: var(--color-text-muted); font-size: 0.9rem; }

  /* Inputs Modernos con Label Flotante */
  .input-group {
    position: relative;
    margin-bottom: 1.5rem;
  }

  .input-group input, 
  .input-group textarea,
  .input-group select {
    width: 100%;
    padding: 1rem 0; /* Solo padding vertical, estilo "underline" */
    font-size: 1rem;
    color: #fff;
    background: transparent;
    border: none;
    border-bottom: 1px solid rgba(255,255,255,0.2);
    outline: none;
    transition: all 0.3s ease;
    border-radius: 0; /* Importante para estilo underline */
  }

  /* Estilos específicos para Select */
  .input-group select {
    color: var(--color-text-muted);
  }
  .input-group select option {
    background-color: var(--color-bg);
    color: #fff;
  }

  /* El Label */
  .input-group label {
    position: absolute;
    top: 1rem;
    left: 0;
    font-size: 1rem;
    color: rgba(255,255,255,0.5);
    pointer-events: none;
    transition: 0.3s ease all;
  }

  /* Animación del Label cuando hay foco o texto */
  .input-group input:focus ~ label,
  .input-group input:not(:placeholder-shown) ~ label,
  .input-group textarea:focus ~ label,
  .input-group textarea:not(:placeholder-shown) ~ label {
    top: -10px;
    font-size: 0.75rem;
    color: var(--color-accent);
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  /* La línea de abajo se ilumina */
  .input-group input:focus,
  .input-group textarea:focus,
  .input-group select:focus {
    border-bottom-color: var(--color-accent);
  }

  .btn-icon { margin-left: 0.5rem; transition: transform 0.3s; }
  .btn-primary:hover .btn-icon { transform: translateX(5px); }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    .contact-layout { 
        grid-template-columns: 1fr; 
        gap: 3rem;
    }
    
    .contact-info { text-align: left; } /* Alineado a la izq en mobile también, es más legible */
    
    .boutique-form {
        padding: 2rem 1.5rem;
    }
  }

  /* ======================
     RESPONSIVE (FIXED)
     ====================== */
  @media (max-width: 900px) {
    /* RESET DE SEGURIDAD PARA MOBILE */
    html, body { width: 100vw; overflow-x: hidden; }

    /* 1. Control de la Marca de Agua (Evita que empuje el ancho) */
    .hero-watermark {
      top: 10%;
      left: 50%;
      transform: translateX(-50%) rotate(-5deg);
      width: 100%; /* Contenida */
      justify-content: center;
    }

    /* 2. Control de la Mancha de Luz */
    .hero-bg-accent {
      width: 100%; 
      right: 0;
      top: 0; 
      height: 50vh;
      filter: blur(60px); /* Menos blur para que no desborde tanto */
    }
    
    /* 3. Ajustes de Layout */
    .bento-grid { grid-template-columns: 1fr; }
    .large, .tall { grid-column: span 1; grid-row: span 1; }
    
    .bio-layout { grid-template-columns: 1fr; text-align: center; }
    .divider-accent { margin: 1.5rem auto; }
    .frame-image { max-width: 300px; margin: 0 auto; }
    
    .stats-grid { flex-direction: column; gap: 2rem; }
    
    .hero { text-align: left; padding-top: 6rem; align-items: center; }
    .hero-actions { flex-direction: column; align-items: flex-start; width: 100%; }
    .btn-primary { width: 100%; }
    
    .form-grid { grid-template-columns: 1fr; }

    /* 4. Textos */
    h1 { font-size: 2.8rem; line-height: 1.1; }
    .text-script { display: block; margin-top: 0.5rem; }
    .lead { font-size: 1rem; }
    .hero-label { font-size: 0.7rem; }
  }

  /* ======================
     FOOTER BOUTIQUE
     ====================== */
  .site-footer {
    background-color: rgba(10, 15, 30, 0.4); /* Un poco más oscuro que el fondo */
    border-top: 1px solid rgba(224, 195, 232, 0.1);
    padding: 5rem 0 2rem 0;
    margin-top: auto;
    font-size: 0.95rem;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr; /* Marca más ancha */
    gap: 4rem;
    margin-bottom: 3rem;
  }

  /* Columna Marca */
  .brand-logo { margin-bottom: 1.5rem; }
  .logo-script {
    font-family: var(--font-logo); /* Allura */
    font-size: 2.5rem;
    color: var(--color-accent);
    line-height: 1;
    display: block;
  }
  .logo-subtitle {
    font-family: var(--font-body);
    font-size: 0.7rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: #fff;
    opacity: 0.7;
    margin-left: 5px;
  }
  .brand-mission {
    color: var(--color-text-muted);
    line-height: 1.6;
    max-width: 350px;
    font-size: 0.9rem;
  }

  /* Títulos de Columnas */
  .footer-links h4, 
  .footer-contact h4 {
    color: #fff;
    font-family: var(--font-heading);
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
  }

  /* Links */
  .footer-links ul { list-style: none; padding: 0; margin: 0; }
  .footer-links li { margin-bottom: 0.8rem; }
  .footer-links a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-block;
  }
  .footer-links a:hover {
    color: var(--color-accent);
    transform: translateX(5px); /* Pequeño movimiento elegante */
  }

  /* Contacto */
  .contact-row { display: flex; gap: 1rem; margin-bottom: 1rem; align-items: flex-start; }
  .contact-row .icon { filter: grayscale(1); opacity: 0.7; }
  .contact-row p { margin: 0; color: var(--color-text-muted); line-height: 1.4; }

  /* Bottom Bar */
  .footer-bottom { margin-top: 3rem; }
  .divider { height: 1px; background: rgba(255,255,255,0.05); margin-bottom: 2rem; }
  
  .bottom-flex {
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: rgba(255,255,255,0.3);
    font-size: 0.8rem;
  }
  
  .credits a { color: rgba(255,255,255,0.5); text-decoration: none; transition: 0.3s; }
  .credits a:hover { color: var(--color-accent); }

  /* RESPONSIVE FOOTER */
  @media (max-width: 900px) {
    .footer-content {
        grid-template-columns: 1fr;
        gap: 3rem;
        text-align: center;
    }
    .brand-mission { margin: 0 auto; } /* Centrar texto misión */
    .contact-row { justify-content: center; } /* Centrar items contacto */
    
    .bottom-flex {
        flex-direction: column;
        gap: 1rem;
    }
  }

  /* ======================
     FAQ SECTION (ACORDEÓN)
     ====================== */
  .faq-section { background: rgba(0,0,0,0.1); } /* Fondo sutilmente distinto */
  
  .accordion {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .accordion-item {
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }

  .accordion-header {
    width: 100%;
    background: transparent;
    border: none;
    padding: 1.5rem 0;
    text-align: left;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    color: #fff;
    font-family: var(--font-heading); /* Playfair */
    font-size: 1.2rem;
    transition: all 0.3s;
  }

  .accordion-header:hover {
    color: var(--color-accent);
  }

  .icon-plus {
    font-family: var(--font-body);
    font-size: 1.5rem;
    color: var(--color-accent);
    transition: transform 0.3s ease;
  }

  /* Estado Abierto (Clase .active que pondremos con JS) */
  .accordion-header.active .icon-plus {
    transform: rotate(45deg); /* Se convierte en una X */
  }
  
  .accordion-header.active {
    color: var(--color-accent);
  }

  .accordion-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-out;
    padding-right: 2rem;
  }

  .accordion-content p {
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: 1.5rem;
    font-size: 1rem;
  }

  /* ======================
     STATEMENT SECTION (SIMPLE DARK)
     ====================== */
  .statement-section {
    /* El mismo fondo oscurito que la sección de stats */
    background: rgba(0, 5, 15, 0.3);
    /* Bordes sutiles arriba y abajo para definir la franja */
    border-top: 1px solid rgba(224, 195, 232, 0.1);
    border-bottom: 1px solid rgba(224, 195, 232, 0.1);
    padding: 5rem 0; /* Espaciado cómodo */
    text-align: center;
  }

  .statement-section h2 {
    font-size: 2rem;
    color: #fff;
    margin-bottom: 1.2rem;
    font-weight: 400;
  }

  .statement-lead {
    max-width: 700px; /* Ancho máximo para lectura cómoda */
    margin: 0 auto; /* Centrado horizontal */
    color: var(--color-text-muted);
    font-size: 1.1rem;
    line-height: 1.7;
    font-weight: 300;
  }

  /* Ajuste para que la palabra en cursiva se vea bien */
  .statement-section .text-script {
      display: inline-block;
      transform: translateY(2px); /* Pequeño ajuste óptico */
  }

  /* Ajuste mobile */
  @media (max-width: 900px) {
    .statement-section { padding: 4rem 0; }
    .statement-section h2 { font-size: 1.6rem; line-height: 1.2; }
    .statement-section .text-script { display: block; margin-top: 0.5rem; font-size: 1.8rem !important; }
    .statement-lead { font-size: 1rem; }
  }
</style>

<script>
  // LÓGICA DEL ACORDEÓN
  const accordionHeaders = document.querySelectorAll('.accordion-header');

  accordionHeaders.forEach(header => {
    header.addEventListener('click', () => {
      // 1. Togglear la clase 'active' para girar el icono y cambiar color
      header.classList.toggle('active');

      // 2. Manejar la altura del contenido (Slide Down/Up)
      const content = header.nextElementSibling as HTMLElement;
      
      if (header.classList.contains('active')) {
        content.style.maxHeight = content.scrollHeight + "px";
      } else {
        content.style.maxHeight = "0";
      }

      // (Opcional) Cerrar los otros al abrir uno
      accordionHeaders.forEach(otherHeader => {
        if (otherHeader !== header && otherHeader.classList.contains('active')) {
          otherHeader.classList.remove('active');
          const otherContent = otherHeader.nextElementSibling as HTMLElement;
          otherContent.style.maxHeight = "0";
        }
      });
    });
  });
</script>